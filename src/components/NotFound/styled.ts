import styled from 'styled-components';
import { layout, typography } from '@styles';

export const NotFoundOuter = styled.div``;

export const NotFoundInner = styled.div`
  ${layout.container}
  max-width: 40rem;
  margin: 4rem auto;
  text-align: center;

  ${typography}
`;
